import type { PpfPillarKey } from "@margot/schema";

/** Pillars edited by framework rule forms (wheel / PPF / HP6 / energy ordering). */
export const frameworkRuleFormPillarKeys: PpfPillarKey[] = [
  "personal",
  "professional",
  "financial"
];
